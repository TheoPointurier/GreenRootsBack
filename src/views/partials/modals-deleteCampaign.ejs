<div id="deleteCampaignModal-<%= campaign.id %>" class="modal hidden delete_modal">
  <div class="modal-content">
    <h2>Confirmer la suppression</h2>
    <p>Êtes-vous sûr de vouloir supprimer la campagne n° <%= campaign.id %> :
        <%= campaign.name %> ?</p>
    <div class="button-container">
      <button id="confirmDeleteButton-<%= campaign.id %>" class="delete button"
        onclick="deleteCampaign('<%= campaign.id %>')">Supprimer</button>
      <button class="cancel button" onclick="hideDeleteCampaignModal('<%= campaign.id %>')">Annuler</button>
    </div>
  </div>
</div>